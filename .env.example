# Claude Sessions Dashboard Configuration

# Port to run the server on (default: 3456)
PORT=3456

# Path to Claude Code config directory (default: ~/.claude)
# This is where Claude Code stores session data in the projects/ subdirectory
CLAUDE_CONFIG_DIR=

# Set to "true" to disable session deletion via the UI (default: false)
READONLY=false
